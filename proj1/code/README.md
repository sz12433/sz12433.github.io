**How to Run Code:**

Hello! I thought it would be more intuitive to use a Jupyter notebook for this project, so I have all my code in the main.ipynb file that can be found in the proj1 folder. I've commented out the some of the code cells that I used for testing and experimenting with algorithms and then calling them on different images in our data folder, so now if we just **Run All** cells we should receive an output display of each photo in the Prokudin-Gorskii gallery in its aligned version (I have a for loop to iterate through each image rather than call the algorithm multiple times manually). I'm not fully sure how this might change because the submission called for not submitting any .tif files that I downloaded locally to run thorugh my algorithm, but I think maybe creating a folder with the same naming convention: "cs180 proj1 data" would allow any chosen images in that folder to be run :)

Some notes on proj1 folder files:
- main.ipynb = code
- cs180 proj1 data = image files imported from project spec
- outoput = folder for aligned image outputs
- proj1.html = the code for my website page
- README.txt = documentation